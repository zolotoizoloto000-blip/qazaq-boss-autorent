document.addEventListener('DOMContentLoaded',async()=>{if(!QB.state.cars.length)await QB.load();let id=new URLSearchParams(location.search).get('id');let c=QB.state.cars.find(x=>String(x.id)===String(id))||QB.state.cars[0];let v=document.querySelector('#carView');if(!c){v.innerHTML='<h1>Автомобиль не найден</h1>';return}v.innerHTML=`<div class="page-hero" style="padding-top:10px"><span class="eyebrow">${esc(c.category||'PREMIUM')}</span><h1>${esc(c.name)}</h1></div><div style="display:grid;grid-template-columns:1.5fr 1fr;gap:22px;margin-top:28px" class="car-detail"><div><img src="${esc(carImage(c))}" style="width:100%;border-radius:18px;border:1px solid #2b3139"><div class="chips"><span class="chip">${esc(c.year||'')}</span><span class="chip">${esc(c.gear||'Автомат')}</span><span class="chip">${esc(c.seats||5)} мест</span></div><p class="muted">${esc(c.description||'')}</p></div><form class="panel" id="bookForm"><h2 style="margin-top:0">${Number(c.price||0).toLocaleString('ru-RU')} ₸ <small class="muted">/ сутки</small></h2><input type="hidden" name="carId" value="${esc(c.id)}"><label class="field">Имя<input name="name" required></label><label class="field">Телефон<input name="phone" required></label><div class="form-grid"><label class="field">Получение<input name="date" type="date" required></label><label class="field">Возврат<input name="endDate" type="date" required></label></div><button class="btn gold full" type="submit">Отправить заявку</button><p class="muted" style="font-size:12px">Бронь считается подтверждённой только после ответа менеджера.</p><div id="bookMsg"></div></form></div>`;let form=document.querySelector('#bookForm');form.onsubmit=async e=>{e.preventDefault();let d=Object.fromEntries(new FormData(form));let body={name:d.name,phone:d.phone,service:'Автопрокат',carId:d.carId,carName:c.name,date:d.date,amount:c.price,extra:{endDate:d.endDate}};let m=document.querySelector('#bookMsg');try{let r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});let j=await r.json();if(!r.ok)throw new Error(j.error||'Ошибка');m.className='notice';m.textContent='Заявка отправлена. Менеджер подтвердит бронь.';form.reset()}catch(err){m.className='notice';m.textContent=err.message}}});